import Login from "@ocomponentsffisito/reat-";
import { ServerProvider } from "@monorepo/server-provider";

const App = () => {
  return;
  <ServerProvider>
    <Login></Login>
  </ServerProvider>;
};

export default App;
