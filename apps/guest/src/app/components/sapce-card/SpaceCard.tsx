import { Card, Grid } from "@mui/material";

const spaceCard = () => (
  <Card>
    <Grid borderRadius="5px" container direction="column">
      <Grid item>a</Grid>
      <Grid item>a</Grid>
      <Grid item>a</Grid>
    </Grid>
  </Card>
);

export default spaceCard;
